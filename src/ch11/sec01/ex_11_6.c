/**
 * 기본 11-6 | 문자 입출력 함수 사용 예 4
 */
#include <stdio.h>

void main() {
    char ch;

    printf("문자열을 입력하세요 : ");
    ch = getchar();
    putchar(ch);

    ch = getchar();
    putchar(ch);

    ch = getchar();
    putchar(ch);
}
