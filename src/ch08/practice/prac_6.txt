연습문제 6번


1. int aa[3];으로 정수형 배열 3개를 선언하면 aa[1], aa[2], aa[3]을 사용할 수 있다. (X)
2. int aa[2] = {10, 20, 30};은 옳은 표현이다. (X)
3. strcpy(A, B)는 A의 문자열을 B에 복사하는 함수이다. (X)
    A에 B를 복사함
4. strcat(A, B) 함수에서 A는 반드시 문자열 배열이어야 하지만 B는 문자열 배열이든 문자열 상수든 상관없다. (O)
5. strcmp(A, B)는 A와 B의 아스키코드 값을 비교해서 같으면 1, 다르면 -1을 반환한다. (X)
    같으면 0, A가 더 크면 1, A가 더 작으면 -1 반환
6. C는 3차원 배열까지 지원하고 4차원 이상의 배열은 지원하지 않는다. (X)
