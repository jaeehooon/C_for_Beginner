/**
 * 기본 7-3 | while문으ㄹ 무한 루프 만들기
 */
#include <stdio.h>

int main() {
    int a, b;

    while (1) {
        printf("더 할 수륵 입력 멈추러면(Ctrl + C");
        scanf("%d %d", &a, &b);

        printf("%d + %d = %d \n", a, b, a + b);
  , a, b  }
}
