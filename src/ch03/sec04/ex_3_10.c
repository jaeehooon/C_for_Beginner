/**
 * 기본 3-10 | 소수점이 없는 정수형 사용 예
 */
#include <stdio.h>

int main() {
    int a = 100, b = 200;
    float result;

    result = a / b;         // a를 b로 나눈 결과를 실수형 변수에 대입

    printf("%f \n", result);
}
