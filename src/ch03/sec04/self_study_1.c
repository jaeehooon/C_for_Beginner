/**
 * Self Study 1 | 기본 3-10의 결과가 0.5가 되도록
 */
#include <stdio.h>

int main() {
    float a = 100, b = 200;
    float result;

    result = a / b;         // a를 b로 나눈 결과를 실수형 변수에 대입

    printf("%.1f \n", result);
}
